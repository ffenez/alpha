# SPECTRUM_VALIDATION_REPORT.md

Научно-технический аудит спектрального анализа по `SPECTRUM_VALIDATION.md`.
Версия приложения на момент аудита — 0.18.1 (versionCode 43), исправления
вошли в 0.19.0.

Воспроизведение на чистом checkout:

```
JAVA_HOME=/home/dev/.local/jdk ./gradlew :app:spectrumValidation
```

Команда прогоняет цепочку на реальном файле и пишет
`app/build/reports/spectrum_validation.json`.

---

## 1. Исходные данные

Файл: `alpha-20260818-005344.xml` (RadiaCode `ResultDataFile`, FormatVersion
120920). Разобран фактически, а не по предполагаемой схеме.

| поле | значение |
|---|---|
| `NumberOfChannels` | 1024 (и столько же `DataPoint` — сходится) |
| `ChannelPitch` | 1 |
| `EnergyCalibration/PolynomialOrder` | 2 |
| `Coefficients` | a0 = 6.8821607, a1 = 2.3377438, a2 = 3.8714128·10⁻⁴ |
| `MeasurementTime` | 36 059 с (10 ч 00 мин 59 с) |
| `StartTime` / `EndTime` | 2026-08-17T14:52:45 / 2026-08-18T00:53:44 (разница 36 059 с — сходится) |
| сумма импульсов | 877 160 |
| `BackgroundEnergySpectrum` | **отсутствует** |
| `SerialNumber` | отсутствует |
| `DeviceConfigReference/Name` | «RadiaCode» — модель не указана |

Следствия, влияющие на расчёт:

- фона в файле нет, поэтому ни вычитание фона, ни нормировка по времени в
  этом разборе не участвуют — синтетический фон не придумывался;
- модель прибора неизвестна, поэтому анализ идёт профилем
  `DeviceModel.UNKNOWN`: разрешение R₆₆₂ = 8,4 % (консервативное), нижняя
  граница поиска пиков 40 кэВ;
- крайний канал 1023 содержит 1315 импульсов — это переполнение шкалы
  (`SpectrumEdge`), в анализ он не входит;
- максимум спектра — канал 32 (82,1 кэВ), 14 113 импульсов.

Обезличенная копия для тестов: `app/src/test/resources/spectra/natural-background-1024ch.xml`
(каналы, калибровка, время накопления; ни идентификаторов, ни отметок времени).
SHA-256 отсчётов (через запятую): `b3c4694e38f988f7e2e238da0a4b588df1c64e91d2c0f16ef43a6c1b10d193b0`.

## 2. Найденные реализации

Текущая цепочка (одна, ветвлений по экранам нет):

| стадия | файл |
|---|---|
| разбор XML | `data/export/RcXml.kt` |
| хранение снимка | `data/db/SpectrumSnapshotEntity` + `SpectrumBlob` |
| канал → энергия | `analysis/EnergyCalibration.kt` |
| край шкалы | `analysis/SpectrumEdge.kt` |
| сглаживание (только вид) | `analysis/SpectrumDisplay.movingAverage` |
| вычитание записанного фона (только вид) | `analysis/SpectrumDisplay.subtractBackground` |
| поиск пиков, континуум, площадь, значимость | `analysis/PeakDetection.kt` |
| совпадение с линиями, вердикты | `analysis/evidence/*`, мост `ui/logic/PeakEvidence.kt` |
| экран и отчёт | `ui/screens/SpectrumScreen.kt`, `SpectrumFullScreen.kt`, `data/export/SpectrumReportFactory.kt` |

Прежняя реализация найдена в истории: коммит `8779465` («Significance that
means something, and numbers that reproduce», 12.08.2026) заменил формулу
значимости и добавил гейт формы. Файл до изменения —
`git show 8779465^:app/src/main/kotlin/app/radiacode/analysis/PeakDetection.kt`.
Различия прежней и текущей версий:

| параметр | прежняя | текущая (до аудита) |
|---|---|---|
| знаменатель значимости | √(B·width + 1) | √(gross + width²·B̂/m) |
| название величины | SNR | значимость |
| гейт формы | нет | FWHM в 0,5–2,5 ожидаемой |
| R₆₆₂ | 8 % константой | параметр модели прибора |
| порог поиска | 40 кэВ константой | `DeviceModel.peakFloorKeV` |

## 3. Один XML — одинаковый вход

Обе реализации прогнаны на ОДНИХ отсчётах. Прежняя — построчной
транскрипцией кода из коммита `8779465^` (её достоверность подтверждается
тем, что независимая транскрипция ТЕКУЩЕЙ реализации совпадает с приложением
до последнего печатного знака: 83,657 / 1430,439 / 2584,549 кэВ и
σ = 43,379 / 20,031 / 5,763).

| реализация | найденные пики |
|---|---|
| прежняя (R₆₆₂ = 8 %) | 84,5 кэВ σ_old = 57,2; 1429,6 кэВ σ_old = 29,0; 2583,6 кэВ σ_old = 9,0 |
| текущая до аудита | 84,5 кэВ σ = 43,4; 1429,2 кэВ σ = 20,0 |
| текущая после аудита | 83,66 кэВ σ = 43,4; 1430,4 кэВ σ = 20,0; **2584,5 кэВ σ = 5,8** |

## 4. Первая точка расхождения

Сырые отсчёты, калибровка, энергии каналов, границы окон, валовые импульсы и
континуум у прежней и текущей версий совпадают **в точности**. Первое
расхождение — на стадии «неопределённость → значимость» и на стадии «гейт
формы»:

1. **значимость**: те же нетто-импульсы делятся на разные знаменатели
   (57,2 против 43,4 на 84,5 кэВ; 29,0 против 20,0 на 1429 кэВ);
2. **состав найденных пиков**: линия 2583 кэВ, которую прежняя версия
   находила, текущей отбраковывалась гейтом формы.

Числа из задания (≈180σ и ≈94σ) **не воспроизводятся** ни одной из
реализаций на этом файле: максимум, который даёт даже самая оптимистичная
прежняя формула, — 57σ. Значит, те значения получены на другом спектре или
другой программой; как ориентир они использованы, как эталон — нет.

## 5. Root cause

**R1. Гейт формы мерил шум.** Наблюдаемая ширина считалась спуском до
половины высоты ОДНОГО центрального канала по сырым отсчётам. При нескольких
импульсах на канал «полувысота» — случайная величина, и первый же провал
обрывает спуск на соседнем канале. У структуры 2614,5 кэВ (Tl-208) с
континуумом 2,4 имп/канал наблюдаемая ширина получалась 5 каналов при
ожидаемых 36 — линия отбраковывалась как одноканальный выброс. Это
ложноотрицательный механизм ровно там, где импульсов меньше всего, — на
высоких энергиях.

**R2. Континуум под окном считался плоским.** Уровень (полусумма средних двух
боковых полос) верен для площади, но внутри окна континуум наклонный, и
вычитание одного уровня оставляет остаточную ступеньку, которая тянет
центроиду в сторону более высокого края. На синтетике с известной истиной
(континуум падает в e раз за 180 каналов) смещение составляло −4,1 кэВ на
1460,8 кэВ и −5,1 кэВ на 662 кэВ; с линейным континуумом — −0,3 и −0,1 кэВ.

**R3. Значимость прежней версии завышала уверенность.** Знаменатель
√(B·width) — шум ТОЛЬКО фона: не учтена ни пуассоновская статистика самих
импульсов пика, ни неопределённость ОЦЕНКИ континуума по боковым полосам.
Это и есть источник расхождения «43σ против 57σ» и «20σ против 29σ». Текущая
формула корректна (вывод ниже), прежняя — нет.

**R4. Разность «образец минус фон» шла в поиск как пуассоновский спектр.**
В проверке продукта (`ExperimentRepository`) линии ищутся в разности, но
дисперсия разности равна `sample + r²·background`, а не значению разности.
Знаменатель значимости был занижен.

**R5. Отчёт и экран разбирали снимок разными параметрами.**
`SpectrumReportFactory` звал поиск с R₆₆₂ = 8,0 % и порогом 40 кэВ по
умолчанию, экран — профилем неопознанного прибора (8,4 %). Для одного снимка
получались слегка разные пики.

## 6. Математическая проверка

Обозначения: окно пика — каналы i ± h, ширина W = 2h + 1; боковые полосы —
по 2h каналов с каждой стороны, всего m каналов; B̂ — оценка континуума на
канал; G — валовые импульсы в окне.

```
нетто:      N = G − W·B̂
дисперсия:  Var(N) = Var(G) + W²·Var(B̂)
            Var(G) = G                      (пуассон, счёт независимых событий)
            Var(B̂) = B̂/m                   (среднее m пуассоновских каналов)
            ⇒ Var(N) = G + W²·B̂/m
значимость: S = N / √Var(N)
```

Ручной пересчёт из сырых отсчётов (проверка в
`SpectrumValidationTest.significance recomputed by hand from the raw counts agrees`):

| пик | i | h | G | B̂ | W | N | σ(N) | S |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 83,7 кэВ | 33 | 4 | 125 109 | 11 827,94 | 9 | 18 657,6 | 430,1 | 43,38 |
| 1430,4 кэВ | 557 | 15 | 2 100 | 34,47 | 31 | 1 031,5 | 51,50 | 20,03 |
| 2584,5 кэВ | 951 | 18 | 173 | 2,375 | 37 | 85,1 | 14,77 | 5,76 |

Прежняя формула на тех же числах: 18 657,6/√(11 827,94·9 + 1) = 57,2;
1 031,5/√(34,47·31 + 1) = 31,5. Разница — ровно вклад пропущенных источников
дисперсии.

**Участок без пика** (контроль): каналы 600–680 (1550–1750 кэВ) — ни одного
кандидата выше 4σ (тест `a region without a peak yields nothing`).

**Применимость нормального приближения.** Для всех трёх пиков валовые
импульсы ≥ 173, относительная асимметрия пуассона 1/√G ≤ 7,6 % — приближение
применимо, знаменатель законно называется σ. Введён явный порог
`MIN_GROSS_COUNTS = 20`: ниже него «σ» не считается вовсе, потому что
распределение не гауссово.

## 7. Calibration

Формула приложения: `E = a0 + a1·ch + a2·ch²`, индексация с нуля, значение —
центр канала, вычисления в Float (коэффициенты из файла, ~7 значащих цифр,
потеря точности ≤ 10⁻⁴ кэВ). Повторного применения калибровки нет: энергия
считается один раз, из центроиды.

| канал | импульсы | E, кэВ |
|---:|---:|---:|
| 0 | 1 293 | 6,88 |
| 32 | 14 113 | 82,09 |
| 100 | 2 550 | 244,53 |
| 557 | 78 | 1429,12 |
| 951 | 4 | 2580,20 |
| 1022 | 1 | 2800,42 |
| 1023 | 1 315 | 2803,55 (край шкалы, из анализа исключён) |

Проверка по известным линиям — независимым измерением (экспоненциальный
континуум по каналам вне окна ± 0,75 FWHM, центроида по избытку):

| линия | E ожидаемая | E измеренная | Δ, кэВ | Δ, % | значимость |
|---|---:|---:|---:|---:|---:|
| Tl-208 583,2 | 583,2 | 584,6 | +1,4 | +0,24 | 8,5 |
| Bi-214 609,3 | 609,3 | 595,5 | −13,8 | −2,27 | 5,9 |
| Bi-214 1120,3 | 1120,3 | 1120,0 | −0,3 | −0,02 | 14,2 |
| K-40 1460,8 | 1460,8 | 1442,7 | −18,1 | −1,24 | 27,9 |
| Tl-208 2614,5 | 2614,5 | 2599,4 | −15,1 | −0,58 | 7,4 |

Ниже 400 кэВ континуум растёт слишком круто для экспоненциальной модели
(нетто выходит отрицательным), и эти линии для сверки шкалы непригодны.

**Вывод по калибровке:** систематического сдвига, который приложение могло бы
исправить, данные не показывают: отклонения разного знака и величины
(+0,24 % … −2,27 %) при статистической неопределённости центроиды порядка
1 кэВ. Это свойство заводской калибровки прибора и метода измерения
положения, а не ошибка приложения. Коэффициенты под 1460,8 кэВ не
подгонялись.

**Почему область K-40 показывается как 1429–1443 кэВ.** Положение зависит от
окна и модели континуума, и разброс оценок укладывается в это же окно:

| метод | E, кэВ |
|---|---:|
| центроида ±h, плоский континуум (было) | 1429,2 |
| центроида ±h, линейный континуум (стало) | 1430,4 |
| подгонка гауссианой + линейный фон, окна 520…610 | 1429,1 … 1434,7 |
| центроида ±0,75 FWHM вокруг ОЖИДАЕМОГО канала, экспоненциальный фон | 1442,7 |

Значение R₆₆₂ на положение почти не влияет: при 6 … 12 % центроида остаётся
1427,6 … 1429,7 кэВ. Версии приложения, различающиеся только константой
разрешения, дать 1441 кэВ не могли — такое значение получается лишь при
другом окне (или на другом накоплении), и по этому файлу оно не
воспроизводится.

## 8. Peaks

Диагностика принятых пиков после исправлений (полностью — в
`spectrum_validation.json`):

| E, кэВ | канал | окно, каналы | G | B̂/канал | наклон, имп/канал | N | σ(N) | S | FWHM набл. | FWHM ожид. |
|---:|---:|---|---:|---:|---:|---:|---:|---:|---:|---:|
| 83,66 | 33 | 29…37 | 125 109 | 11 827,9 | +114,10 | 18 657,6 | 430,1 | 43,4 | 28,4 кэВ | 19,8 кэВ |
| 1430,44 | 557 | 542…572 | 2 100 | 34,47 | −0,181 | 1 031,5 | 51,5 | 20,0 | 49,8 кэВ | 81,7 кэВ |
| 2584,55 | 951 | 933…969 | 173 | 2,375 | −0,017 | 85,1 | 14,8 | 5,8 | не измерена | 109,9 кэВ |

Реальность структуры 2584 кэВ проверена независимо от алгоритма: континуум
вокруг (каналы 880…1020 без 925…985) описан экспонентой, избыток в окне
933…969 составил 103 импульса при √G = 13,2, то есть 7,9σ; в двух контрольных
окнах той же ширины избытки +1,0σ и −2,6σ. Линия есть.

## 9. Significance

Почему получались разные σ — см. §5 (R3) и §6. Корректным признано
`S = N/√(G + W²·B̂/m)`; прежнее `N/√(B·W)` завышало значимость в 1,3–1,6 раза
на этом спектре и называлось «SNR», хотя знаменатель не был ни шумом сигнала,
ни неопределённостью площади.

Дополнительно введено:

- `MIN_GROSS_COUNTS = 20` — граница применимости нормального приближения;
- для спектров-разностей — покональная дисперсия
  `Var = sample + r²·background` (параметр `variance` в `PeakDetection.detect`).

## 10. Isotope matching

Совпадения ищутся ПОСЛЕ определения энергии и её неопределённости; допуск —
z-оценка с σ, включающей ширину линии прибора, неопределённость центроиды и
табличную неопределённость энергии (`analysis/evidence/EnergyMatching.kt`).

Вердикты на этом спектре:

| нуклид | вердикт | линий найдено / проверено | неразрешимость |
|---|---|---:|---|
| K-40 | WEAK (возможное совпадение) | 1 / 1 | — |
| Tl-208 | WEAK | 1 / 3 | — |
| Eu-152 | CONTRADICTED | 1 / 9 | K-40 |
| La-138 | CONTRADICTED | 1 / 2 | K-40 |

Пик 83,66 кэВ нуклиду не приписан: он объяснён характеристическим рентгеном
K-серии тяжёлых элементов (Pb 84,9 · Bi 87,3 кэВ) — механизмом, который не
требует нового нуклида. Ни один нуклид не объявлен найденным: потолок
формулировок — «возможное совпадение», и он держится тестами.

## 11. Исправления

| файл | что изменено |
|---|---|
| `analysis/PeakDetection.kt` | линейный континуум под окном; гейт формы применяется только при измеримой высоте центра, иначе ширина = null; отдельная проверка «один канал несёт больше половины нетто» (защита от выброса больше не зависит от измеримости ширины); порог `MIN_GROSS_COUNTS`; необязательная поканальная дисперсия |
| `data/ExperimentRepository.kt` | в поиск линий разности передаётся дисперсия `sample + r²·background` |
| `data/export/SpectrumReportFactory.kt` | снимок разбирается тем же профилем неопознанного прибора, что и экран |
| `app/build.gradle.kts` | задача `:app:spectrumValidation` |

Не менялось (проверено и признано корректным): разбор XML, хранение и
неизменность сырых отсчётов, формула калибровки, исключение крайнего канала,
формула значимости, нормировка записанного фона по времени, независимость
анализа от вида (лог/лин/степень, сглаживание, наложение фона).

## 12. Tests

`app/src/test/kotlin/app/alpha/analysis/SpectrumValidationTest.kt` — реальный
файл: поля XML, канал → кэВ, неизменность сырых отсчётов после полного
анализа, состав и числа пиков, ручной пересчёт значимости, участок без пика,
запись `spectrum_validation.json`, внешняя сверка по K-40 и Tl-208.

`app/src/test/kotlin/app/alpha/analysis/PeakScienceTest.kt` — синтетика с
заранее известной истиной: широкая слабая линия на единицах импульсов
находится; одноканальный выброс отбраковывается; центроида на круто падающем
континууме не смещена (среднее по 12 спектрам |Δ| < 2 кэВ); нетто-площадь
воспроизводит вложенную (76 % площади гауссианы в окне ±FWHM/2); две линии в
159 кэВ друг от друга не сливаются; чистый континуум не даёт пиков; спектр в
единицы импульсов не даёт «σ»; спектр с большой статистикой держит положение
линии.

Прогон:

```
./gradlew test          → BUILD SUCCESSFUL (все JVM-тесты :app и :protocol)
./gradlew :app:smokeTest → BUILD SUCCESSFUL
./gradlew :app:lintDebug → BUILD SUCCESSFUL
./gradlew :app:spectrumValidation → отчёт app/build/reports/spectrum_validation.json
```

## 13. Remaining uncertainty

Чего по этому XML доказать нельзя:

1. **Числа 180σ и 94σ.** Ни одной комбинацией параметров прежней или текущей
   реализации они не получаются. Скорее всего относятся к другому накоплению
   или другой программе; без исходного файла того измерения вопрос закрыт
   быть не может.
2. **Абсолютная точность энергетической шкалы.** Отклонения измеренных
   положений от табличных (+0,24 % … −2,27 %) не складываются в
   монотонную зависимость, и по одному спектру нельзя разделить вклад
   заводской калибровки, наложения соседних линий (1429 кэВ — это ещё и
   La-138 1435,8 и Ac-228 1459,1) и метода измерения положения.
3. **Абсолютные площади.** Эффективность регистрации прибора неизвестна,
   поэтому нетто-площадь не переводится в активность — и приложение этого не
   делает.
4. **Фон.** В файле его нет; поведение цепочки «вычитание фона → поиск»
   проверено синтетикой и покональной дисперсией, но не этим измерением.
5. **Низкоэнергетическая область.** Ниже 400 кэВ континуум не описывается ни
   плоской, ни линейной, ни экспоненциальной моделью в пределах окна, поэтому
   положение структуры 83,7 кэВ имеет систематику порядка кэВ, которую этот
   файл не позволяет измерить.
